# 会议转录非中文语言（英文 / 俄语）误识别 — 诊断与优化方案

> 排查对象：`realtime_meeting`（实时会议转录 + 翻译）
> 场景：高质量会议模板 + 两人中文闲聊，结果出现英文 / 俄语识别
> 结论先行：**主因是"语言先验级联污染"，而非模型能力本身**。一旦某一段被误判为英文/俄语，会话级全局语言状态会把它当成语种先验，强制后续所有片段（含另一说话人）都用错误语种解码，形成自我强化循环。

---

## 0. 关键代码链路（先建立共识）

| 环节 | 文件 / 行 | 作用 |
|------|-----------|------|
| 模板定义 | `web/app.js:70-99` | "高质量会议" 参数：realtime_beam 8 / refine_beam 10 / best_of 9 / no_speech_threshold 0.55 |
| 语种集合 | `language.py:6` | `SUPPORTED = {"zh","en","de"}`（**不含 ru**） |
| 检测器 | `language.py:38-78` `MultilingualDetector.detect` | 文本正则 + lingua 判定；**信任 Whisper 当 `conf>=0.65`** |
| 语种先验归一 | `runtime.py:763-769` `_normalize_asr_language` | 仅 zh/en/de 有效，其余返回 None |
| 强制语种解码 | `runtime.py:820-825` `_whisper_decode` | `if normalized_language: options["language"]=...`（**强制按先验语种解码**） |
| 先验来源 | `runtime.py:966,1016` `_recognize` | `language_hint = 上一段的语种` |
| 会话级全局语种 | `session.py:110` `current_language` → `:682` 更新 → `:672/:752` 回灌 | **单一全局变量，无按说话人区分** |
| 提示词也按语种过滤 | `session.py:673/:753` + `_recent_asr_context`(`:466-470`) | 提示词只取"当前语种"的历史，进一步强化 |

---

## 1. 模板配置维度

**现象**：切换到"高质量会议"后语言误识别并未改善，甚至略增。

**原因**：
- 高质量模板只调了 beam / best_of / threshold，**完全没动语言相关逻辑**。语种级联污染在所有模板下都发生。
- `no_speech_threshold` 从默认 0.6 **降到 0.55**（`app.js:89`），让 Whisper 更倾向于把模糊/短/低能量的音频当成语音 → 产生更多低置信片段 → 更多误识别机会。
- 模板里**没有"锁定语种 / 限制候选语种"**这类开关。对"两人中文闲聊"这种已知单语种场景，本应直接锁 zh。
- `enable_refinement=True` 时，精修路径（large-v3）同样使用 `previous_language` 先验（`runtime.py:1143/:752`），级联同样发生。

**优化方向**：
1. 新增会议级设置 `asr_language_lock`（如 `zh` | `auto`）。中文闲聊直接设为 `zh`，跳过 Whisper 自动识别，彻底消除误判。
2. 新增 `asr_language_candidates`（候选语种白名单）。即使 auto，也只让 Whisper 在 {zh,en,de} 内选，杜绝 ru 等意外语种。
3. 语言相关参数（候选集、先验信任阈值）纳入模板定义，使"高质量会议"对语种更保守。

---

## 2. 语言检测机制维度（最核心）

**现象**：中文被标英文，且偶发整段俄语（西里尔字母）文本。

**原因（逐条对照代码）**：
- **过度信任 Whisper 置信度**：`language.py:49-51` 当 `whisper_confidence >= 0.65` 且 hint≠zh 时，直接采用 Whisper 结论。而 large-v3 **对英文极度自信、对中文偏置弱**，短句/带口音的中文常被它-report 成 "en" 且概率 ≥0.65，于是检测器照单全收。
- **俄语无支持 → 文本与标签错位**：ru 不在 `SUPPORTED`（`language.py:6`），`normalize_language_code("ru")` 返回 None → hint 为空 → 检测器兜底分支（`language.py:74`）返回 `LanguageGuess(上一段语种 or "en", 0.25)`。也就是说：**西里尔字母文本被贴上"英文/中文"标签**，这就是你"看到俄语被识别"的本质——Whisper 吐出西里尔字符，但系统没有 ru 这个桶，于是标签错配。
- **纯文本判定、无声学信号**：检测器只看文本正则 + lingua（且 lingua 也只装了 zh/en/de）。短中文、夹英文、少汉字的片段它无法纠正。
- **无时间平滑 / 无投票**：每段独立判定，单个坏片段就能永久翻转 `current_language`，没有迟滞（hysteresis）也没有"最近 N 段多数票"。

**优化方向**：
1. **提高先验信任门槛 + 要求佐证**：Whisper 报告非中文时，必须同时满足（CJK 字符存在）或（置信度极高 **且** 检测器文本判定一致）才采用；否则视为"待定"，走自动重识别（`language=None` 再解一次）。
2. **滑动窗口投票 + 迟滞**：维护最近 K 段语种多数票，翻转语种需连续 M 段一致，单段不翻转。
3. **语种白名单外强制重解**：Whisper 返回不在 {zh,en,de} 的语种时，直接以 `language=None` 重解（已有 retry 机制，但仅在"需要重试"时触发；应专门对"语种不在白名单"触发）。
4. **用更稳的 LID**：引入 fasttext `lid.176` 或 `langid`，覆盖多文种/多字母表；或加一层 Unicode Script 检测（西里尔→直接标"未知/非目标语种"而非错位成 en）。

---

## 3. 声学模型匹配维度

**现象**：比之前好些，但英文/俄语仍混入。

**原因**：
- 实时模型是 `large-v3-turbo`（`prepare_models.py:84`，配置 `config.py:43`），精修是 `large-v3`（`config.py:45`）。两者都是**通用多语种大模型**。
- `large-v3-turbo` 是蒸馏加速版，**语言识别与非英文鲁棒性弱于满血版**，更易把中文误判英文/吐西里尔。高质量模板把实时 beam 拉到 8，但** acoustic 模型本身的语种偏置是天花板**。
- 没有"中文专用"双模型策略：中文会议用通用大模型，等于让一个以英文语料占优的模型去解码中文。

**优化方向**：
1. **中文会议换中文优化模型**：实时模型可换 SenseVoice-small / Paraformer / Whisper-zh 微调版（FunASR 体系已在该项目 VAD 中使用，`prepare_models.py` 已依赖 funasr），精修保留 large-v3 或同样换中文模型。
2. **强制语种偏置**：即便用 Whisper，也在解码时 `language="zh"`（已知中文会议），从 tokenizer 层面压住英文/西里尔。
3. **混合策略**：实时用中文小模型保低延迟 + 高语种纯度，精修用大模型提准确率，互不污染。

---

## 4. 背景噪声 / 方言口音维度

**现象**：闲聊场景（可能咖啡厅/办公室），口音重时误识别增多。

**原因**：
- `audio.py` 的 `StreamSegmenter._is_speech` 有能量门（`minimum_rms`）+ 自适应 `noise_floor`，但噪声地板更新很慢（`0.98*old + 0.02*rms`，`audio.py:141`）。在空调/风扇/键盘噪声下，稳态噪声可能被模型 VAD 当成语音切出片段 → 强迫 Whisper 把噪声解码成"某种语言" → 产生英文/俄语噪声 token。
- 高质量模板 `vad_minimum_speech_ms=600`、`vad_minimum_speech_ratio=0.16`（`app.js:79-80`）：片段更长，但若噪声混入，长片段里夹带错误语种。
- **口音/方言**：large-v3-turbo 对方言、南北口音、夹英文覆盖有限，声学匹配弱 → 退回其强势的英文先验。

**优化方向**：
1. **前端降噪**：进 ASR 前接 RNNoise / WebRTC NS / AGC，降低稳态噪声。
2. **频谱级 VAD 门控**：不只看能量，结合过零率/谱质心，过滤稳态噪声。
3. **短/低能量片段不喂先验**：低于某长度或能量阈值的片段，用 `language=None` 自动识别，避免用错误先验强制解码。
4. **口音**：中文专项模型 + 中文 bias 提示词，明显优于通用大模型。

---

## 5. 双说话人场景下的语言切换误判维度（最严重）

**现象**：一人误判，另一人也跟着变；会话中 zh/en 反复横跳。

**原因（根因中的根因）**：
- `session.py:110` 的 `current_language` 是**整个会话单一全局变量**，`:682` 用"上一段的语种"更新它，`:672/:752` 把它作为 `previous_language` 回灌给**下一段**，`_recent_asr_context(self.current_language)`（`:673/:753`，过滤逻辑 `:469-470`）又用它筛选提示词。
- **没有按说话人维度存语种**。两个说话人共用一个全局先验：说话人 A 首段被误判 "en" → 全局锁 en → 说话人 B 的中文也被强制按英文解码。
- 提示词也按当前语种过滤 → 一旦锁 en，喂给 Whisper 的 `initial_prompt` 是英文文本 → **进一步强化英文解码**，形成自循环。
- 即便某段正确出 zh（有 CJK），检测器返回 zh 并更新全局；但下一段若短、无 CJK，Whisper 又高置信报 en → 再次翻转。于是单场中文对话里 zh/en 闪烁。

**优化方向（按收益排序）**：
1. **语种状态按说话人隔离**：维护 `speaker_languages[speaker_id]`，只把"该说话人"的语种作为先验回灌。这一步即可切断跨说话人污染——两人中文闲聊时任一人的误判不再传染对方。
2. **迟滞 + 多数票锁语种**：需连续 M 段一致才翻转；单段坏数据不改锁。
3. **已知单语种会议直接锁 zh**：暴露"锁定语言"开关，`language="zh"` 全覆盖、提示词也锁 zh，最稳。
4. **提示词语种与解码语种解耦**：即使需要 auto，也不要用"全局当前语种"去过滤提示词；提示词可固定中文（中文会议）或用多语种混合。

---

## 6. 优先级行动清单（建议实施顺序）

| 优先级 | 改动 | 文件 | 预期收益 |
|--------|------|------|----------|
| P0 | 语种状态按说话人隔离 + 迟滞多数票 | `session.py`（`current_language`→`speaker_languages`）、`runtime.py` 回灌逻辑 | 切断跨说话人污染，消除 zh/en 闪烁 |
| P0 | 新增"锁定语言 / 候选白名单"设置并接入模板 | `config.py`、`web/app.js`、`session.py` 调用处 | 中文闲聊直接归零误识别 |
| P1 | 提高 Whisper 先验信任门槛 + 白名单外强制重解 | `language.py:49-51`、相关重解触发 | 减少英文/俄语误收 |
| P1 | 中文会议换中文优化模型（实时） | `prepare_models.py`、`config.py` | 从声学层面降低偏置 |
| P2 | 前端降噪 + 频谱 VAD 门控 | `audio.py` | 降低噪声触发误识别 |
| P2 | 更稳 LID（fasttext/langid）+ Script 检测 | `language.py` | 解决 ru 错配与未知语种 |

---

## 7. 一句话总结

你的"英文/俄语误识别"不是单纯模型烂，而是 **`session.py` 用单一全局语种状态 + `runtime.py` 把它当 Whisper 解码先验 + `language.py` 过度信任 Whisper 置信度** 三者叠加的级联污染；高质量模板没修这个点反而因更低 `no_speech_threshold` 略增片段数。**最快见效的两步**：① 给"中文闲聊"类会议加"锁定语言=zh"开关；② 把语种状态从会话级全局改为按说话人隔离并加迟滞。做完这两点，你描述的场景基本可根治。

> 需要我直接落地 P0 的两处改动（按说话人隔离语种 + 锁定语言设置接入模板）吗？可以基于现有代码结构给出可运行的补丁。
